/* -------------------------------------------------------------------------------------------------

Load this stylesheet after formtastic.css in your layouts to override the CSS to suit your needs.
This will allow you to update formtastic.css with new releases without clobbering your own changes.

For example, to make the inline hint paragraphs a little darker in color than the standard #666:

form.formtastic fieldset ol li p.inline-hints { color:#333; }

--------------------------------------------------------------------------------------------------*/
/* Start Forms */
form.formtastic fieldset {
  width: 34em;
  font-size: 14pt; }
form.formtastic fieldset legend,
form.formtastic label,
form.formtastic input,
form.formtastic select,
form.formtastic select option,
form.formtastic textarea {
  font-family: Georgia;
  font-size: 14pt;
  font-color: #000; }
form.formtastic input,
form.formtastic select,
form.formtastic textarea {
  border-top: 2px solid #999999;
  border-left: 2px solid #999999;
  border-right: 2px solid #cccccc;
  border-bottom: 2px solid #cccccc;
  font-size: 14pt; }
form.formtastic input[type=text],
form.formtastic input[type=password],
form.formtastic textarea {
  width: 20em; }
form.formtastic select {
  height: 1.5em; }
form.formtastic input[type=submit],
form.formtastic input[type=button] {
  background: #229316;
  color: #fff;
  border-top: 2px solid #55c649;
  border-left: 2px solid #55c649;
  border-right: 2px solid #006000;
  border-bottom: 2px solid #006000;
  margin: 0 5px; }
form.formtastic input[type=submit][value=cancel] {
  border-top: 2px solid #e68582;
  border-left: 2px solid #e68582;
  border-bottom: 2px solid #801f1c;
  border-right: 2px solid #801f1c;
  color: #ffb8b5;
  background: #b3524f; }
form.formtastic div.fieldWithErrors {
  display: inline; }
