%h3
  Add Item to Donor
%div.left_half
  - @donor_item = @the_thing
  - form_for(:donor_item, :url => donor_donor_items_path(@donor), :html => {:method => :post}) do |f|
    %fieldset
      %table
        = render :partial => "form", :locals => {:f => f}
      = f.submit "Create"
      |
      = link_to 'Cancel', edit_polymorphic_path([@donor])
