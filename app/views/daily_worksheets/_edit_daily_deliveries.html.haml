%div.boxtitle
  Deliveries and Pickups
-# use {:style => "overflow: scroll"} to cause the div to contain the items (no auto overflow), but put scrollbars
%div.boxcontent
  %table{{ :border => 1, :style => "border-style:inset"}}
    %tr
      %th 
        = 'Order'
      %th 
        = 'Delivery/Pickup comments'
      %th 
        = 'Directions'
      %th 
        = 'Address'
        %br
        = 'Buzz / Level / Elevator?'
      %th 
        = 'Name and Number'
      %th 
        = 'PU/DEL (Class)'
      %th 
        = 'Items'
      %th
        = 'Action'
    %tbody{:id => "delivery_list"}
      - f.fields_for :daily_deliveries do |dd_form|
        = render :partial => 'edit_daily_delivery', :locals => {:dd_form => dd_form}
      -# have 3 extra blank delivered item rows shown, so they can be easily added (to avoid js)
      -# (7 blank item rows if this is a totally new delivery being entered)
      - 1.times do  
        - f.fields_for :daily_deliveries, DailyDelivery.new do |dd_form|
          = render :partial => 'edit_daily_delivery', :locals => {:dd_form => dd_form}
      
